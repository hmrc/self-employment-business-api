put:
  $ref: "./create_amend_cumulative_period_summary.yaml"

  security:
    - User-Restricted:
        - write:self-assessment

get:
  $ref: "./retrieve_cumulative_period_summary.yaml"

  security:
    - User-Restricted:
        - read:self-assessment
