put:
  $ref: "./create_and_amend_annual_submission.yaml"

  security:
    - User-Restricted:
        - write:self-assessment

get:
  $ref: "./retrieve_annual_submission.yaml"

  security:
    - User-Restricted:
        - read:self-assessment

delete:
  $ref: "./delete_annual_submission.yaml"

  security:
    - User-Restricted:
        - write:self-assessment
